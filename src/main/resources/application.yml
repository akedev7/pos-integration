spring:
  application:
    name: pos
  datasource:  # Required for Flyway
    url: jdbc:postgresql://localhost:5432/appdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    validate-on-migrate: true
    locations: classpath:db/migration